class Solution {
    public String reverseVowels(String s) {
        int left=0;
        int right=s.length()-1;
        int temp=0;
        while(left<right){
            char c=s.charAt(left).toLowerCase();
            char d=s.charAt(right).toLowerCase();
            if(c=='a'||c=='e'||c=='i'||c=='o'||c=='u'&&d=='a'||d=='e'||d=='i'||d=='o'||d=='u'){
                
            }
            
        }
    }
}